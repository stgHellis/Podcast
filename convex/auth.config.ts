// https://tops-marten-8.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://tops-marten-8.clerk.accounts.dev/",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
